// bakr: a daemon that keeps long-lived agents alive inside a directory it is
// handed (not one it mints). This is a scaffold — see README.md. No claim
// store, spawn substrate, daemon process, or agent lifecycle yet.
export {};
